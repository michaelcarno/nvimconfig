return {
  "taybart/b64.nvim",
  event = "VeryLazy",
  config = function() end,
  keys = {
    -- { "g[", function() require("b64").encode() end, desc = "encode to base64" },
    -- { "g]", function() require("b64").decode() end, desc = "dencode to base64" },
  },
}
