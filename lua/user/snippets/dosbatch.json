{

	"buildbat":{
	  "prefix":"latexBuildBat",
	  "body":[
    "latexmk -lualatex -bibtex-cond   -shell-escape    -emulate-aux-dir    -aux-directory=build    -synctex=1    -interaction=nonstopmode     -file-line-error    -outdir=build    \"./.tex\" "
	  ]
	}
}
